hello-fastify has been deployed.

{{- if .Values.ingress.enabled }}
Host:          {{ .Values.ingress.host }}
Entrypoint:    {{ .Values.ingress.entryPoint }} (plain HTTP, no TLS)

The Service and Pods only ever speak plain HTTP on port {{ .Values.service.targetPort }}.
{{- else }}
Ingress is disabled — reach the service in-cluster via:
  {{ include "hello-fastify.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}
{{- end }}
